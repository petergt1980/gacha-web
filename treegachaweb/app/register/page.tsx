'use client';import{useState}from'react';import{useRouter}from'next/navigation';export default function Register(){const[n,setN]=useState(''),[e,setE]=useState(''),[p,setP]=useState('');const r=useRouter();async function go(x){x.preventDefault();const z=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:n,email:e,password:p})});if(z.ok)r.push('/dashboard')}return <main className="wrap"><div className="panel" style={{maxWidth:440,margin:'70px auto'}}><h1>Register</h1><form onSubmit={go}><input className="input" placeholder="Username" value={n} onChange={x=>setN(x.target.value)}/><input className="input" placeholder="Email" value={e} onChange={x=>setE(x.target.value)}/><input className="input" type="password" placeholder="Password" value={p} onChange={x=>setP(x.target.value)}/><button className="btn">Register</button></form></div></main>}
