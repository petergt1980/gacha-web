import {NextResponse} from 'next/server'; import bcrypt from 'bcryptjs'; import {db} from '../../../../lib/db'; import {sign} from '../../../../lib/auth';
export async function POST(req){const {name,email,password}=await req.json();if(!name||!email||!password||password.length<6)return NextResponse.json({error:'Invalid data'},{status:400});if(await db.user.findUnique({where:{email}}))return NextResponse.json({error:'Exists'},{status:409});const u=await db.user.create({data:{name,email,password:await bcrypt.hash(password,12)}});const res=NextResponse.json({ok:true});res.cookies.set('tree_token',await sign(u.id),{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',maxAge:604800,path:'/'});return res}
