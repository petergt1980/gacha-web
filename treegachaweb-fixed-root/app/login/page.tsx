'use client';import{useState}from'react';import{useRouter}from'next/navigation';export default function Login(){const[e,setE]=useState(''),[p,setP]=useState(''),[m,setM]=useState('');const r=useRouter();async function go(x){x.preventDefault();const z=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:e,password:p})});if(z.ok)r.push('/dashboard');else setM('Login gagal')}return <main className="wrap"><div className="panel" style={{maxWidth:440,margin:'70px auto'}}><h1>Login</h1><form onSubmit={go}><input className="input" placeholder="Email" value={e} onChange={x=>setE(x.target.value)}/><input className="input" type="password" placeholder="Password" value={p} onChange={x=>setP(x.target.value)}/><button className="btn">Login</button></form><p className="muted">{m}</p><a href="/register">Create account</a></div></main>}
